# Чеклист настройки и запуска

## ✅ Быстрая проверка готовности

### Базовая настройка (для V1)
- [ ] Python 3.11+ установлен
- [ ] Virtual environment создан и активирован
- [ ] `pip install -r requirements.txt` выполнен
- [ ] `.env` файл создан и заполнен
- [ ] `config.ini` настроен
- [ ] Telegram бот создан и добавлен в канал
- [ ] YouTube OAuth настроен
- [ ] Gemini API ключ получен
- [ ] Папки созданы (resources/music, resources/fonts)
- [ ] Шрифты добавлены в resources/fonts/
- [ ] Музыка добавлена в resources/music/

### Дополнительная настройка (для V2)
- [ ] `selenium` установлен
- [ ] `opencv-python` установлен
- [ ] ChromeDriver скачан и добавлен в PATH
- [ ] ffmpeg установлен и добавлен в PATH
- [ ] HTML шаблон `resources/templates/news_short_v2.html` существует

## 📋 Пошаговая настройка

### Шаг 1: Установка Python зависимостей
```bash
# Активация venv
.\venv\Scripts\Activate.ps1  # Windows
source venv/bin/activate     # Linux/macOS

# Установка пакетов
pip install -r requirements.txt
```
**Ожидаемый результат:** Все пакеты установлены без ошибок

---

### Шаг 2: Настройка .env файла
```bash
# Создайте .env файл с вашими ключами
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_BOT_TOKEN=ваш_bot_token
GEMINI_API_KEY=ваш_gemini_key
YOUTUBE_CLIENT_SECRET_FILE=client_secret_xxx.json
```
**Ожидаемый результат:** Файл .env существует и содержит все ключи

---

### Шаг 3: Выбор версии генератора
Откройте `config.ini`:
```ini
[VIDEO]
generator_version = v1  # или v2
```

**Для V1 (MoviePy) - рекомендуется для начала:**
- ✅ Быстрая генерация
- ✅ Меньше зависимостей
- ✅ Проверенная стабильность

**Для V2 (HTML+Selenium) - для продвинутых анимаций:**
- ⚠️ Требует ChromeDriver
- ⚠️ Медленнее генерация
- ✨ Красивые анимации

---

### Шаг 4: Тестирование
```bash
# Тест обеих версий (если V2 настроен)
python test_video_versions.py

# Или тест в песочнице
python run_sandbox_test.py
```
**Ожидаемый результат:** 
- Видео созданы в `outputs/test/`
- Нет критических ошибок в логах

---

### Шаг 5: Запуск основного скрипта
```bash
python main_script.py
```
**Ожидаемый результат:** 
- Скрипт запущен
- Подключение к Telegram успешно
- YouTube OAuth пройден (при первом запуске)

---

## 🔧 Устранение проблем

### Проблема: "Module not found"
```bash
# Проверьте что venv активирован
pip list

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Проблема: "GEMINI_API_KEY not set"
```bash
# Проверьте .env файл
cat .env  # Linux/macOS
type .env  # Windows

# Убедитесь что в .env есть строка:
GEMINI_API_KEY=ваш_ключ
```

### Проблема: V2 не работает (Selenium)
```bash
# Установите selenium
pip install selenium

# Скачайте ChromeDriver
# https://chromedriver.chromium.org/downloads
# Версия должна совпадать с версией Chrome

# Проверьте версию Chrome
# chrome://version/

# Добавьте ChromeDriver в PATH или в папку проекта
```

### Проблема: Видео без звука (V2)
```bash
# Установите ffmpeg
# Windows: https://ffmpeg.org/download.html
# Linux: sudo apt install ffmpeg
# macOS: brew install ffmpeg

# Проверьте установку
ffmpeg -version
```

### Проблема: Telegram бот не получает сообщения
```bash
# Проверьте:
# 1. Бот добавлен в канал как администратор
# 2. У бота есть права на чтение сообщений
# 3. ID канала правильный (начинается с -100)
# 4. Токен бота корректный

# Проверьте логи
tail -f logs/*.log  # Linux/macOS
Get-Content logs\*.log -Wait  # Windows
```

---

## 📊 Финальная проверка

Перед запуском в production:

- [ ] Тесты пройдены успешно
- [ ] Видео создаются без ошибок
- [ ] YouTube загрузка работает (или отключена в dev)
- [ ] Логи пишутся в logs/
- [ ] Конфиг соответствует вашим потребностям
- [ ] Выбрана подходящая версия генератора (v1/v2)
- [ ] Резервные копии созданы

---

## 🚀 Быстрый старт (TL;DR)

```bash
# 1. Клон и установка
git clone <repo>
cd Telegram-to-YouTube-Shorts
python -m venv venv
.\venv\Scripts\Activate.ps1  # Windows
pip install -r requirements.txt

# 2. Настройка
# Создайте .env с ключами
# Отредактируйте config.ini

# 3. Тест
python test_video_versions.py

# 4. Запуск
python main_script.py
```

---

## 📚 Дополнительные ресурсы

- **Полная документация:** [VIDEO_DESIGN_ARCHITECTURE.md](VIDEO_DESIGN_ARCHITECTURE.md)
- **Быстрый старт V2:** [QUICK_START_V2.md](QUICK_START_V2.md)
- **Миграция:** [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)
- **Песочница:** [DEV_SANDBOX.md](DEV_SANDBOX.md)

---

**Готово!** Если все пункты отмечены, проект готов к работе! 🎉

