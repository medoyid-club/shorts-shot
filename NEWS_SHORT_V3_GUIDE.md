# 🎨 Руководство по использованию news_short_v3.html

## Обзор

`news_short_v3.html` — улучшенная версия шаблона с современными анимациями и визуальными эффектами, полностью совместимая с существующим Python-кодом.

## ✨ Новые возможности

### 1. **GSAP анимации**
- Плавное появление всех элементов с эффектом `back.out`
- Stagger анимация для красных блоков (появление по очереди)
- Timeline для синхронизации всех анимаций

### 2. **Визуальные эффекты**
- **Shine эффект** на желтом заголовке (как на BBC News)
- **Пульсирующее свечение** вокруг желтого блока
- **Параллакс эффект** для медиа (плавное движение и зум)
- **Градиентные фоны** вместо плоских цветов
- **Backdrop blur** для водяного знака

### 3. **Улучшенный дизайн**
- Радиальный градиент фона (темный → черный)
- Градиентные красные блоки с тенями
- Улучшенные тени и границы для глубины
- Плавные переходы между элементами

### 4. **Fallback поддержка**
- Если GSAP не загрузится, используются CSS анимации
- Полная совместимость с существующим кодом

## 🚀 Быстрый старт

### Шаг 1: Переключение на новый шаблон

Откройте `config.dev.ini` и измените путь к шаблону:

```ini
[VIDEO]
# Старый шаблон (базовый дизайн)
# v2_template_path = resources/templates/news_short_v2.html

# Новый шаблон (премиум дизайн с GSAP)
v2_template_path = resources/templates/news_short_v3.html
```

### Шаг 2: Запуск песочницы

```bash
python run_sandbox_test.py
```

### Шаг 3: Отправка тестового сообщения

Отправьте сообщение в тестовый Telegram канал. Система автоматически использует новый шаблон.

## 🔧 Технические детали

### Совместимость

✅ **Полностью совместим** с существующим Python-кодом:
- Использует те же плейсхолдеры: `{{NEWS_TITLE}}`, `{{NEWS_BRIEF}}`, `{{NEWS_IMAGE}}`, `{{NEWS_VIDEO}}`, `{{SOURCE_NAME}}`
- Та же логика обработки медиа
- Та же генерация красных блоков из текста

### Зависимости

- **GSAP 3.12.5** — загружается через CDN (автоматически)
- Если CDN недоступен, используются CSS fallback анимации

### Производительность

- GSAP анимации оптимизированы для производительности
- Параллакс эффект использует CSS `transform` (GPU-ускорение)
- Минимальное влияние на скорость захвата кадров

## 🎨 Настройка анимаций

### Изменение скорости анимаций

В файле `news_short_v3.html` найдите секцию с GSAP timeline:

```javascript
// Увеличить скорость всех анимаций
const tl = gsap.timeline({ duration: 0.5 }); // Быстрее

// Или изменить отдельные анимации
tl.from('#titleBlock', {
    duration: 0.5, // Было 0.8
    ease: 'back.out(1.7)'
}, 0.5);
```

### Изменение эффектов

#### Отключить shine эффект:
```css
.title-block::before {
    display: none; /* или animation: none; */
}
```

#### Изменить параллакс медиа:
```css
@keyframes zoomPan {
    0% { transform: scale(1) translate(0, 0); }
    100% { transform: scale(1.05) translate(-10px, -10px); } /* Меньше движения */
}
```

#### Изменить цвета градиентов:
```css
.title-block {
    background: linear-gradient(135deg, #FF6B6B, #FFE66D, #FF6B6B); /* Оранжево-желтый */
}
```

## 📊 Сравнение версий

| Функция | v2 (базовый) | v3 (премиум) |
|---------|--------------|--------------|
| CSS анимации | ✅ | ✅ |
| GSAP анимации | ❌ | ✅ |
| Shine эффект | ❌ | ✅ |
| Параллакс медиа | ❌ | ✅ |
| Градиентные фоны | ❌ | ✅ |
| Пульсирующее свечение | ❌ | ✅ |
| Fallback поддержка | ✅ | ✅ |
| Производительность | ⚡⚡⚡ | ⚡⚡ |

## 🐛 Решение проблем

### GSAP не загружается

**Симптом:** Анимации работают, но без GSAP эффектов (используется fallback)

**Решение:**
1. Проверьте интернет-соединение (GSAP загружается с CDN)
2. Или скачайте GSAP локально и подключите:
   ```html
   <script src="resources/js/gsap.min.js"></script>
   ```

### Анимации слишком быстрые/медленные

**Решение:** Измените `duration` в GSAP timeline или CSS `animation-duration`

### Медиа не анимируется

**Решение:** Убедитесь, что медиафайл загружен. Параллакс применяется только к загруженным элементам.

## 💡 Рекомендации

1. **Для продакшн:** Используйте v3 для более профессионального вида
2. **Для тестирования:** Можно быстро переключаться между v2 и v3 через конфиг
3. **Для кастомизации:** Все эффекты легко настраиваются через CSS и JavaScript

## 🔄 Возврат к старому шаблону

Если нужно вернуться к v2:

```ini
[VIDEO]
v2_template_path = resources/templates/news_short_v2.html
```

---

**Готово!** Теперь ваши шортсы будут выглядеть более профессионально и современно! 🎉

