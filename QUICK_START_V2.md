# Быстрый старт с генератором V2

## Что нового?

Добавлен новый генератор видео V2 с HTML+Selenium:
- ✨ Красивые анимации через CSS
- 🎨 Полный контроль над дизайном
- 📱 Современный новостной формат
- ⚡ Отдельные LLM промпты

## Установка зависимостей

### 1. Установите Python пакеты:

```bash
# Активируйте venv
.\venv\Scripts\Activate.ps1

# Обновите зависимости
pip install -r requirements.txt
```

### 2. Установите ChromeDriver:

**Вариант A (автоматически):**
```bash
pip install webdriver-manager
```

**Вариант B (вручную):**
1. Узнайте версию Chrome: `chrome://version/`
2. Скачайте ChromeDriver: https://chromedriver.chromium.org/downloads
3. Распакуйте и добавьте в PATH

### 3. Убедитесь что ffmpeg установлен:

```bash
# Проверка
ffmpeg -version

# Если нет, скачайте с https://ffmpeg.org/download.html
# И добавьте в PATH
```

## Быстрый тест

### Запустите тестовый скрипт:

```bash
python test_video_versions.py
```

Этот скрипт:
- ✅ Проверит работу обеих версий (V1 и V2)
- ✅ Создаст тестовые видео в `outputs/test/`
- ✅ Покажет отчет о работе

## Переключение между версиями

### В config.ini (production):

```ini
[VIDEO]
generator_version = v2  # или v1
```

### В config.dev.ini (development):

```ini
[VIDEO]
generator_version = v2  # Новый дизайн для экспериментов
v2_headless = false     # Видим браузер при разработке
```

## Запуск в песочнице

```bash
# Активируйте venv
.\venv\Scripts\Activate.ps1

# Запустите sandbox с V2
python run_sandbox_test.py
```

## Что делать если V2 не работает?

### Ошибка "selenium is not installed"
```bash
pip install selenium
```

### Ошибка "ChromeDriver not found"
```bash
# Проверьте версию Chrome
chrome://version/

# Скачайте соответствующий ChromeDriver
# https://chromedriver.chromium.org/downloads

# Добавьте в PATH или положите в папку проекта
```

### Ошибка "cv2 not found"
```bash
pip install opencv-python
```

### Видео создается без звука
```bash
# Установите ffmpeg
# https://ffmpeg.org/download.html

# Добавьте в PATH
```

### Браузер не открывается
```ini
# В config.dev.ini отключите headless режим
[VIDEO]
v2_headless = false
```

## Откат на V1

Если V2 не работает, просто переключитесь обратно:

```ini
[VIDEO]
generator_version = v1
```

Все будет работать как раньше!

## Кастомизация дизайна V2

Отредактируйте HTML шаблон:

```
resources/templates/news_short_v2.html
```

Изменяйте:
- 🎨 Цвета (CSS переменные)
- 🔤 Шрифты (Google Fonts)
- ✨ Анимации (@keyframes)
- 📐 Layouts (flexbox, grid)

## Следующие шаги

1. **Протестируйте V2:** `python test_video_versions.py`
2. **Настройте дизайн:** Отредактируйте `resources/templates/news_short_v2.html`
3. **Запустите в dev:** `python run_sandbox_test.py`
4. **Сравните результаты:** Проверьте видео в `outputs/test/` и `outputs/dev/`
5. **Переключитесь в production:** Измените `generator_version = v2` в `config.ini`

## Документация

Подробная документация в файле: **VIDEO_DESIGN_ARCHITECTURE.md**

## Помощь

Если возникли проблемы:
1. Проверьте логи (logs/)
2. Запустите `python test_video_versions.py` для диагностики
3. Убедитесь что все зависимости установлены
4. Попробуйте отключить headless режим для отладки

---

**Готово!** 🎉 Теперь вы можете использовать оба генератора и переключаться между ними.

